using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class EnforceButton : MonoBehaviour
{
    [SerializeField] Image[] EnforceImage = null;

    void Start()
    {
       
       //button = Inventory.Instance.dice.
    }

    private void Update()
    {
        
        //SetImage(Inventory.Instance.dice.)
    }

    void SetImage(Sprite sprite)
    {
        //EnforceImage.GetComponent<Image>().sprite = sprite;
    }
}
